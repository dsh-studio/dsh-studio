/**
 * DSH Studio provider presets — config-only bundle. All behavior lives in
 * cordis.patch.yml (llm-pi-ai provider routes); no runtime code.
 */

/** No host-side behavior; the bundle patch is the whole contribution. */
export function apply() {}
